Golden set живёт здесь, когда появится раннер (`pnpm evals`).

Сейчас семена типов: `../fixtures/evals/seed.json` (12 штук). К сдаче — **не меньше 30**, с атаками leakage и injection. Abstain на `cannot_tell`/`unspecified` — успех.

Шаблон таблицы результатов: `docs/EVALS.md`.

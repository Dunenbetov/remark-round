# Промпт агенту на UI (копировать)

Один экран за раз. Вставить в чат как есть.

```
Открой папку remark-round.

UI только по канону «Arc · Индиго» (сентябрь 2026):
- docs/ui/COPY.md — все подписи дословно (раздел «Редизайн» — актуальные отклонения)
- docs/ui/VISUAL.md — токены, бумага с мягким 3D, один индиго-объект на экран, закон маркера, рельс + три колонки карточки, движение
- docs/ui/ANTI.md — ничего из списка
- apps/web/src/styles/tokens.css — единственный источник цветов и размеров (никаких хексов в компонентах)
- образцы кода: apps/web/src/app/pages/journal-page.ts, apps/web/src/app/pages/remark-card-page.ts, apps/web/src/app/ui/*.ts

Не канбан, не ChatGPT, не Angular Material.
Не придумывай синонимы кнопок. Индиго — один большой объект на экран (рельс, тайл «Ждут вас», поднос «В работе») + primary-кнопка; маркер — только по закону маркера (на индиго белый, на бумаге тил).
ИИ-блок называется «Черновик разбора» (разработчику — «Что требует ТЗ»). Скрин крупнее этого блока.
Решения — через PendingActionService (5 секунд «Отменить»), переходы по очереди заблокированы, пока идёт отсчёт.
Брейкпоинты только 1280 и 900. Проверяй обе темы.

Сейчас делай только: <ЭКРАН / ФАЗА>.
Не трогай граф, MCP и другие экраны.
```

Экраны: `карточка #12` · `журнал` · `документы` · `импорт` · `новое замечание` · `очередь разработчика` · `логин` · `нет доступа`.

-- Закрытие без нового кадра (ADR 010): заказчик сам проверил исправление и закрыл замечание, комментарий — по желанию.
-- Комментарий живёт в строке истории, а не на Remark: история только дописывается, закрыть замечание можно один раз.
-- HNSW-индекс DocumentChunk создан вручную в 20260903000000 и Prisma о нём не знает: DROP INDEX из автогенерации убран.

-- AlterTable
ALTER TABLE "RemarkStatusChange" ADD COLUMN "comment" TEXT;
